# CI test failures on 2026-09-23

## Scope

This report covers completed failed `CI` workflow runs from 08:37 through 22:12 UTC on 2026-09-23.
It records failed tests from GitHub Actions logs and separates timeouts from other errors.
The runs used different commits, so a failure on a pull request does not prove that the same test fails on the current main branch.

## Findings and changes

| Failure | Evidence | Change or finding |
| --- | --- | --- |
| A chat link test timed out while it rendered an asset image, and a second test timed out while it checked four lazy renderers in one callback. | [35926809138](https://github.com/Maxsky5/openducktor/actions/runs/35926809138), [35916643866](https://github.com/Maxsky5/openducktor/actions/runs/35916643866), [35905399759](https://github.com/Maxsky5/openducktor/actions/runs/35905399759) | The file link cases no longer repeat the rich image render. Each final renderer now has its own test. The rich renderer test still includes an asset image. |
| Combined Markdown tests timed out during real Mermaid and premium code renders. | [35838222083](https://github.com/Maxsky5/openducktor/actions/runs/35838222083), [36029951506](https://github.com/Maxsky5/openducktor/actions/runs/36029951506), [36031332474](https://github.com/Maxsky5/openducktor/actions/runs/36031332474) | The math, code, and asset test repeated checks that separate tests cover, so it was removed. The math and fenced code test now uses plain code without theme setup. A separate test renders real syntax without a file-wide preload. Separate tests still check asset resolution and real Mermaid output. |
| A cold syntax render and full onboarding and chat renders exceeded one second in parallel CI on 2026-09-24. | [36040866394](https://github.com/Maxsky5/openducktor/actions/runs/36040866394), [36041625049](https://github.com/Maxsky5/openducktor/actions/runs/36041625049) | The syntax test has a four-second budget after a measured 2.93-second macOS run. Four full render tests have narrow 2.5- or 3-second budgets. The parallel test scripts did not change. |
| A Windows task asset test spent 30 seconds in its live process probe. | [35838222083](https://github.com/Maxsky5/openducktor/actions/runs/35838222083), [36031332474](https://github.com/Maxsky5/openducktor/actions/runs/36031332474) | The Windows PowerShell path now has a direct test with an injected command runner. That test checks the command, output parser, and invalid output without starting PowerShell. The live-owner test uses the native current-process path on Windows and a child process on Linux and macOS. The second CI run showed that a real Windows PowerShell probe took about four seconds, so it does not run in the normal test suite. |
| Full notification, dev server hook, and Electron log tests exceeded the default one-second budget. | [35900824991](https://github.com/Maxsky5/openducktor/actions/runs/35900824991), [35841358409](https://github.com/Maxsky5/openducktor/actions/runs/35841358409), [35926218367](https://github.com/Maxsky5/openducktor/actions/runs/35926218367), [35844135677](https://github.com/Maxsky5/openducktor/actions/runs/35844135677), [36029951506](https://github.com/Maxsky5/openducktor/actions/runs/36029951506) | Notification kind groups now skip renders when only capability or test status changes. The full integration checks retain 2.5-second per-test budgets. Each timeout has a cause comment. |
| A Windows Git fixture hook exceeded five seconds before a test could start. | [35912204745](https://github.com/Maxsky5/openducktor/actions/runs/35912204745) | The hook starts Git and writes two commits. It has a 10-second hook budget. |
| An Electron dev-script test exited its fake process before the CDP port file was observed. | [36031332474](https://github.com/Maxsky5/openducktor/actions/runs/36031332474) | The fake process now exits when the endpoint is logged. The test no longer uses a 500-millisecond polling deadline. |
| Many unrelated tests timed out together on one Linux run. | [35841619806](https://github.com/Maxsky5/openducktor/actions/runs/35841619806) | This points to runner contention, but the logs do not identify one cause. The same branch later passed in [35857091295](https://github.com/Maxsky5/openducktor/actions/runs/35857091295). No suite-wide timeout or parallelism setting changed. |

## Failed run inventory

| Run | Platform | Failed tests or error | Status in this change |
| --- | --- | --- | --- |
| [35926980401](https://github.com/Maxsky5/openducktor/actions/runs/35926980401) | Linux, Windows | Linux repeated the notification form and chat link asset timeouts. Windows failed `createOpenCodeWorkspaceRuntimeStarter > includes startup timeout cleanup failures` with `EBUSY` during cleanup. | Notification budget and chat render changes apply. The Windows cleanup error is a separate process lifecycle failure. |
| [35926809138](https://github.com/Maxsky5/openducktor/actions/runs/35926809138) | Linux, Windows | Linux timed out in the chat link asset case and `the final lazy renderers all retain the file action`. Windows repeated the non-directory path assertion and three native symlink import timeouts. | Chat renderer tests reduced. The Windows tests came from the external-session pull request. The assertion is not a timeout. |
| [35926218367](https://github.com/Maxsky5/openducktor/actions/runs/35926218367) | Linux | `useAgentStudioDevServerPanel subscriptions > binds query and mutation results to the browser transport epoch` timed out. | Per-test budget. |
| [35916643866](https://github.com/Maxsky5/openducktor/actions/runs/35916643866) | macOS | `chat Markdown links > [file](src/a.ts)` with an asset image timed out. | Duplicate rich render removed. |
| [35915815216](https://github.com/Maxsky5/openducktor/actions/runs/35915815216) | Windows | `worktree removal path resolution > propagates a non-directory path error` failed an assertion; `Workspace Session commands with real Git and SQLite > imports, retries after archive save, and restores a native symlink path` timed out, then cleanup got `EBUSY`. | These tests came from the external-session pull request. The assertion is not a timeout. |
| [35912819816](https://github.com/Maxsky5/openducktor/actions/runs/35912819816) | Windows | The same worktree path assertion failed; the external-session tests for archive save and branch snapshot save timed out while restoring a native symlink path. | These tests came from the external-session pull request. |
| [35912204745](https://github.com/Maxsky5/openducktor/actions/runs/35912204745) | Windows | `settings modal sidebars > renders all settings sections including chat` timed out; an unnamed `Workspace Session commands with real Git and SQLite` hook timed out and cleanup got `EBUSY`. | Git fixture hook budget changed. The sidebar render takes about 6 ms in the focused local run, so no timeout was added to it. |
| [35905399759](https://github.com/Maxsky5/openducktor/actions/runs/35905399759) | macOS, Windows | macOS repeated the chat link asset timeout. Windows failed `createOpenCodeWorkspaceRuntimeStarter > removes a registered live adapter when forwarding startup fails` with `EBUSY` during cleanup. | Duplicate rich render removed. The Windows cleanup error is a separate process lifecycle failure. |
| [35900824991](https://github.com/Maxsky5/openducktor/actions/runs/35900824991) | Linux | `SettingsNotificationsSection > offers Windows system settings without a permission result` and `SettingsNotificationsSection > opens system settings and rechecks denied Electron notification permission` timed out. | Per-test budgets. |
| [35895038923](https://github.com/Maxsky5/openducktor/actions/runs/35895038923) | Linux | 33 frontend tests failed in delegation, task operations, pull request operations, refresh, and runtime checks. The exact names appear below. | This pull request later passed in [35907640219](https://github.com/Maxsky5/openducktor/actions/runs/35907640219). The failures were not all timeouts. |
| [35844135677](https://github.com/Maxsky5/openducktor/actions/runs/35844135677) | Windows, Linux | Windows had four unnamed frontend hook timeouts and one Electron log timeout. Linux failed to load `keytar` because `libsecret-1.so.0` was missing; those module errors had no test names. | Electron log budget changed. The Linux load error is not a test timeout. |
| [35841619806](https://github.com/Maxsky5/openducktor/actions/runs/35841619806) | Linux | 13 frontend tests and two generated-image worker tests failed. The exact names appear below. | Most timed out in the same period. Two frontend assertions failed after other timeouts. |
| [35841358409](https://github.com/Maxsky5/openducktor/actions/runs/35841358409) | macOS | `SettingsNotificationsSection > offers Windows system settings without a permission result` timed out. | Per-test budget. |
| [35838222083](https://github.com/Maxsky5/openducktor/actions/runs/35838222083) | Windows | `node task asset file port > keeps staging for a live owner when the start-time probe uses local ps output` and `rich task description rendering > composes math, premium code, and task assets; Mermaid=%s` timed out. One frontend hook had no name. | The Windows command has a direct fake-runner test. The repeated combined Markdown test was removed. |

### The 33 frontend failures in run 35895038923

- `useDelegationOperations`: refreshes the delegated task scope after a successful build start; rejects a delegated build when no Builder default exists; rejects a delegated build when the catalog lacks the Builder default; rejects a delegated build when the Builder default runtime catalog fails.
- `useTaskOperations`: composes reset commands with workspace guards before reading session metadata; runs reset commands through dependency-local ports and refreshes task state; reports and rethrows reset command failures through the notification port.
- `useTaskPullRequestOperations`: detects and invalidates CI review data after linking an open pull request; links a detected merged pull request through the injected cleanup and invalidates CI review data; unlinks a pull request and invalidates CI review data; warns and does not link when pull request detection finds no pull request; reports pull request detection failures without rethrowing to the UI handler; ignores merged pull request detection that completes after a repository switch; invalidates merged pull request state on repository switch and reports expired links; reports link failures and clears the linking state; delegates unlink failures to the mutation runner and clears unlinking state; does not cancel a merged pull request while linking starts in the same turn.
- `task refresh flow`: does not invoke a refresh without an active repo.
- `useTaskResetOperations`: reports a session refresh failure without rejecting a successful reset; reports both refresh failures without rejecting a successful task reset; invalidates stop impact after a failed reset attempt.
- `use-checks`: refreshChecks is a no-op when no active repo is selected; refreshRuntimeCheck caches and supports force retries; does not refresh runtime health while mounted; does not own runtime health loading when runtime definitions become available after mount; tracks per-repo task-store cache when active repo changes; deduplicates runtime health error toasts across manual refreshes; shows cli and task-store toasts for unhealthy successful payloads; refreshChecks starts independent probes in parallel; refreshChecks forces a fresh runtime check and surfaces errors; refreshChecks waits for all failed probes before surfacing unavailable diagnostics; refreshChecks times out hung probes and clears loading state; projects runtime and task-store query timeouts into concrete states instead of leaving checks pending.

### The 15 named failures in run 35841619806

- `task description Markdown dialect`: semantically round-trips common formatting; semantically round-trips simple and aligned GFM tables; semantically round-trips compound list math with two math blocks in one bullet.
- `Pierre CodeView editor continuity`: keeps the attached editor document, selection, and undo history across Save state.
- `OnboardingPage`: shows the app version and theme control in the onboarding header.
- `useOnboardingRuntimeSetup`: keeps Continue available after an optional runtime rediscovery fails; checks only the runtime whose executable path changed.
- File preview and dialog tests: forced repository removal cancels a pending dirty target change; failed file read after dialog replacement cannot show an old error; failed worktree read after dialog replacement cannot show an old error; Escape closes the clean file preview before the transcript; `TaskExecutionSelectedFilePreview > returns focus to Pierre after Keep editing and routes both decisions`; Escape cannot close a saving preview and works again after success.
- Generated-image worker tests: wrong-kind preserves diagnostics, stops its worker, and permits later work; send preserves diagnostics, stops its worker, and permits later work.

### Unnamed hooks in run 35844135677

- Windows frontend logs placed the four unnamed hook failures in `use-workspace-branch-operations.test.tsx`, `use-task-delete-dialog.test.tsx`, `use-agent-studio-shell-task-actions.test.tsx`, and `use-agent-chat-rendered-transcript.test.ts`.
- The earlier Windows run [35838222083](https://github.com/Maxsky5/openducktor/actions/runs/35838222083) placed one unnamed hook failure in `task-details-async-document-section.test.tsx`.

## Verification limits

The focused tests and full repository checks ran on macOS in this worktree.
The first [2026-09-24 CI run](https://github.com/Maxsky5/openducktor/actions/runs/36029951506) passed on Linux and Windows.
The macOS job in that run timed out in the combined Markdown test, the denied-permission notification test, and a ModelPicker focus tooltip test.
The second [2026-09-24 CI run](https://github.com/Maxsky5/openducktor/actions/runs/36031332474) passed on Linux, timed out in the Windows math and premium code test, and failed a macOS Electron dev-script test because its fake process exited before the port file appeared.
The new Windows probe test checks the PowerShell command through a fake runner. It does not start PowerShell in hosted CI.
The earlier timeout changes passed on Linux, macOS, and Windows in two sequential CI runs: [36032701472](https://github.com/Maxsky5/openducktor/actions/runs/36032701472) and [36033469979](https://github.com/Maxsky5/openducktor/actions/runs/36033469979).
Both runs used the normal parallel test scripts and completed the build and generated-image worker checks.
The follow-up changes passed the full Linux and macOS CI jobs in [36042885878](https://github.com/Maxsky5/openducktor/actions/runs/36042885878).
The changed frontend tests also passed on Windows in that run, but the Windows job failed `worktree removal path resolution > propagates a non-directory path error` in the host suite.
That assertion also failed in [36040866394](https://github.com/Maxsky5/openducktor/actions/runs/36040866394) and [36041625049](https://github.com/Maxsky5/openducktor/actions/runs/36041625049), and it is not a timeout.
