{
  "name": "@openducktor/build-tools",
  "version": "0.6.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "bunx tsc -p tsconfig.json",
    "typecheck": "bunx tsc -p tsconfig.json --noEmit",
    "test": "bun test --pass-with-no-tests --timeout 1000",
    "lint": "oxlint -c ../../.oxlintrc.json src"
  },
  "devDependencies": {
    "bun-types": "^1.3.14"
  }
}
