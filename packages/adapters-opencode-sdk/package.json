{
  "name": "@openducktor/adapters-opencode-sdk",
  "version": "0.6.0-beta.3",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "bunx tsc -p tsconfig.json",
    "typecheck": "bunx tsc -p tsconfig.json --noEmit",
    "test": "bun test --pass-with-no-tests --timeout 1000 && node --experimental-strip-types tests/opencode-command-fetch.node.mjs",
    "lint": "oxlint -c ../../.oxlintrc.json src"
  },
  "dependencies": {
    "@opencode-ai/sdk": "^1.18.25",
    "@openducktor/contracts": "workspace:*",
    "@openducktor/core": "workspace:*",
    "@openducktor/path-support": "workspace:*",
    "undici": "^8.10.0",
    "zod": "^4.4.3"
  }
}
