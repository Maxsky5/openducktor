name: Release Desktop

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag to publish (for example v0.1.0)"
        required: true
        type: string

concurrency:
  group: ${{ github.workflow }}-${{ inputs.tag }}
  cancel-in-progress: false

env:
  BUN_VERSION: 1.3.11
  CARGO_TERM_COLOR: always
  XCODE_APP_PATH: /Applications/Xcode_26.2.app

jobs:
  build-macos:
    name: Build ${{ matrix.label }} desktop assets
    permissions:
      contents: write
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: macOS Apple Silicon
            runner: macos-15
            target: aarch64-apple-darwin
          - label: macOS Intel
            runner: macos-15-intel
            target: x86_64-apple-darwin

    steps:
      - name: Checkout release tag
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: ${{ inputs.tag }}

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: ${{ env.BUN_VERSION }}

      - name: Resolve release metadata
        id: release_meta
        env:
          INPUT_TAG: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          TAG="$INPUT_TAG"
          if [[ ! "$TAG" =~ ^v(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)(-[0-9A-Za-z.-]+)?$ ]]; then
            echo "Release tag must look like v0.1.0 or v0.1.0-rc.1." >&2
            exit 1
          fi

          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "version=${TAG#v}" >> "$GITHUB_OUTPUT"

      - name: Verify draft release exists
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.release_meta.outputs.tag }}
        run: |
          set -euo pipefail
          if ! gh release view "$TAG" >/dev/null 2>&1; then
            echo "Draft release $TAG was not found. Run Prepare Release first or rerun this workflow manually with the correct tag." >&2
            exit 1
          fi

          IS_DRAFT="$(gh release view "$TAG" --json isDraft --jq '.isDraft')"
          if [[ "$IS_DRAFT" != "true" ]]; then
            echo "Release $TAG already exists and is not a draft. Desktop artifacts should only attach to a draft release." >&2
            exit 1
          fi

      - name: Install JS dependencies
        run: bun install --frozen-lockfile

      - name: Select Xcode 26.2 SDK
        run: |
          set -euo pipefail
          if [[ ! -d "$XCODE_APP_PATH" ]]; then
            echo "Expected Xcode at $XCODE_APP_PATH on the GitHub macOS runner." >&2
            exit 1
          fi

          sudo xcode-select -s "$XCODE_APP_PATH"
          echo "DEVELOPER_DIR=$XCODE_APP_PATH/Contents/Developer" >> "$GITHUB_ENV"
          echo "SDKROOT=$(xcrun --sdk macosx --show-sdk-path)" >> "$GITHUB_ENV"

      - name: Report active Apple toolchain
        run: |
          set -euo pipefail
          xcodebuild -version
          xcrun --sdk macosx --show-sdk-version
          xcrun --find clang

      - name: Install packaged sidecar prerequisites
        run: |
          set -euo pipefail
          if ! command -v bd >/dev/null 2>&1 || ! command -v dolt >/dev/null 2>&1; then
            HOMEBREW_NO_AUTO_UPDATE=1 brew install beads
          fi

      - name: Resolve packaged sidecar binaries
        run: |
          set -euo pipefail
          BD_BINARY="$(command -v bd)"
          DOLT_BINARY="$(command -v dolt)"

          if [[ -z "$BD_BINARY" || -z "$DOLT_BINARY" ]]; then
            echo "Expected bd and dolt to be available for sidecar packaging." >&2
            exit 1
          fi

          echo "OPENDUCKTOR_BD_BINARY=$BD_BINARY" >> "$GITHUB_ENV"
          echo "OPENDUCKTOR_DOLT_BINARY=$DOLT_BINARY" >> "$GITHUB_ENV"

          "$BD_BINARY" --help >/dev/null
          "$DOLT_BINARY" version

      - name: Verify repo version matches tag
        run: bun run release:version:check "${{ steps.release_meta.outputs.version }}"

      - name: Resolve CEF cache metadata
        id: cef_cache
        run: bun run --cwd apps/desktop scripts/resolve-cef-cache-metadata.ts >> "$GITHUB_OUTPUT"

      - name: Cache Bun download cache
        uses: actions/cache@668228422ae6a00e4ad889ee87cd7109ec5666a7 # v5.0.4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ env.BUN_VERSION }}-${{ hashFiles('bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-${{ env.BUN_VERSION }}-

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9 # v1
        with:
          toolchain: stable
          targets: ${{ matrix.target }}

      - name: Cache Rust artifacts
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          workspaces: |
            apps/desktop/src-tauri
          shared-key: desktop-release-${{ matrix.target }}

      - name: Cache OpenDucktor CEF toolchain and runtime
        uses: actions/cache@668228422ae6a00e4ad889ee87cd7109ec5666a7 # v5.0.4
        with:
          path: ${{ steps.cef_cache.outputs.cargo_tauri_root }}
          key: ${{ runner.os }}-${{ runner.arch }}-${{ matrix.target }}-cargo-tauri-${{ steps.cef_cache.outputs.tauri_revision_short }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-${{ matrix.target }}-cargo-tauri-

      - name: Cache export-cef-dir tool
        uses: actions/cache@668228422ae6a00e4ad889ee87cd7109ec5666a7 # v5.0.4
        with:
          path: ${{ steps.cef_cache.outputs.export_cef_tool_root }}
          key: ${{ runner.os }}-${{ runner.arch }}-${{ matrix.target }}-export-cef-dir-${{ steps.cef_cache.outputs.cef_version }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-${{ matrix.target }}-export-cef-dir-

      - name: Cache exported CEF bundle
        uses: actions/cache@668228422ae6a00e4ad889ee87cd7109ec5666a7 # v5.0.4
        with:
          path: ${{ steps.cef_cache.outputs.cef_path }}
          key: ${{ runner.os }}-${{ runner.arch }}-${{ matrix.target }}-cef-runtime-${{ steps.cef_cache.outputs.cef_version }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-${{ matrix.target }}-cef-runtime-

      - name: Verify CEF native build prerequisites
        run: |
          set -euo pipefail
          cmake --version
          ninja --version

      - name: Validate release secrets
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          set -euo pipefail
          missing=()
          for name in APPLE_CERTIFICATE APPLE_CERTIFICATE_PASSWORD APPLE_SIGNING_IDENTITY APPLE_ID APPLE_PASSWORD APPLE_TEAM_ID; do
            if [[ -z "${!name:-}" ]]; then
              missing+=("$name")
            fi
          done

          if [[ ${#missing[@]} -gt 0 ]]; then
            printf 'Missing required desktop release secrets:\n' >&2
            printf ' - %s\n' "${missing[@]}" >&2
            exit 1
          fi

      - name: Install Apple signing certificate
        uses: apple-actions/import-codesign-certs@fe74d46e82474f87e1ba79832ad28a4013d0e33a
        with:
          p12-file-base64: ${{ secrets.APPLE_CERTIFICATE }}
          p12-password: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}

      - name: Prime CEF toolchain
        run: bun run tauri:setup:cef

      - name: Resolve CEF build paths
        id: cef
        run: bun run --cwd apps/desktop scripts/resolve-cef-release-env.ts >> "$GITHUB_OUTPUT"

      - name: Clear stale target bundle outputs
        env:
          TARGET: ${{ matrix.target }}
        run: |
          set -euo pipefail
          rm -rf "apps/desktop/src-tauri/target/$TARGET/release/bundle"
          rm -rf "apps/desktop/src-tauri/target/$TARGET/bundle"

      - name: Build and upload signed desktop assets
        uses: tauri-apps/tauri-action@3a44005a6b66d6af4b709c2f646f1ba40c0541be
        timeout-minutes: 60
        env:
          GITHUB_TOKEN: ${{ github.token }}
          CEF_PATH: ${{ steps.cef.outputs.cef_path }}
          OPENDUCKTOR_TAURI_TARGET: ${{ matrix.target }}
          OPENDUCKTOR_PREPARE_SIDECARS: "1"
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        with:
          tagName: ${{ steps.release_meta.outputs.tag }}
          projectPath: apps/desktop
          tauriScript: ${{ steps.cef.outputs.cargo_tauri_path }}
          args: --target ${{ matrix.target }} --config src-tauri/bundle.sidecars.json --features cef -- --no-default-features
          releaseDraft: true
          uploadUpdaterJson: false
          uploadUpdaterSignatures: false
          retryAttempts: 2

      - name: Verify signed CEF helper in app bundle
        env:
          TARGET: ${{ matrix.target }}
        run: |
          set -euo pipefail
          helper="apps/desktop/src-tauri/target/$TARGET/release/bundle/macos/OpenDucktor.app/Contents/MacOS/openducktor-desktop-helper"

          if [[ ! -f "$helper" ]]; then
            echo "Bundled CEF helper was not found at $helper" >&2
            exit 1
          fi

          codesign --verify --verbose=4 "$helper"
          codesign -dvvv "$helper"
          entitlements_file="$(mktemp)"
          codesign -d --entitlements :- "$helper" > "$entitlements_file"

          require_entitlement() {
            local entitlement="$1"
            local value

            value="$(/usr/libexec/PlistBuddy -c "Print :$entitlement" "$entitlements_file")"
            if [[ "$value" != "true" ]]; then
              echo "Expected entitlement $entitlement=true on bundled CEF helper, got: $value" >&2
              exit 1
            fi
          }

          require_entitlement "com.apple.security.cs.allow-jit"
          require_entitlement "com.apple.security.cs.allow-unsigned-executable-memory"

  finalize-release:
    name: Summarize release draft
    if: ${{ always() }}
    permissions:
      contents: read
    needs:
      - build-macos
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Publish workflow summary
        env:
          BUILD_RESULT: ${{ needs.build-macos.result }}
          GH_REPO: ${{ github.repository }}
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          RELEASE_URL="$(gh release view "$TAG" --json url --jq '.url')"
          {
            echo "## Desktop release draft ready"
            echo
            echo "- Tag: $TAG"
            echo "- Build job result: $BUILD_RESULT"
            echo "- Draft release: $RELEASE_URL"
            echo "- Assets uploaded by tauri-action for macOS arm64 and macOS Intel"
            echo "- Signing/notarization: required"
            echo "- Release notes source: GitHub generated notes using .github/release.yml"
            echo "- Next step: smoke-test the draft assets, then publish the draft in GitHub Releases"
          } >> "$GITHUB_STEP_SUMMARY"
